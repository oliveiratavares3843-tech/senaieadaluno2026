# 1. Adicionar o arquivo com o conflito resolvido
git add index.txt

# 2. Concluir o commit de merge
git commit -m "merge: resolve conflito de h1 entre main e feature1 integrando os termos"

# 3. Enviar todo o histórico atualizado e protegido para o GitHub
git push origin main

